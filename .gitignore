# Secrets - never commit
models.json
auth.json

# Ephemeral data - session history, trust decisions
sessions/
trust.json

# MCP config & cache (mcp.json contains API keys)
mcp.json
mcp-cache.json
mcp-npx-cache.json
mcp-onboarding.json

# Project-local pi config (restored by pi)
.pi/

# Project memory - session context, past decisions, gotchas
MEMORY.md

# Installed pi packages (restored via pi install)
npm/
git/
